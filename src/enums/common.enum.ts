export enum  BooleanEnum {
    False,
    True
}

export enum TimeToLive {
    TenSeconds = 10,
    HaftMinute = 30,
    Minute= 60,
    FiveMinutes = 300,
}

export enum TransactionEnum {
    Buy,
    Sell
}